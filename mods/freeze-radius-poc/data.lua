-- Freeze-radius PoC data-stage entry point. Registers only NEW prototypes (a
-- minimal freeze-carrier planet + the heat-interface radius clones); it never
-- edits vanilla content.
require("prototypes.planet")
