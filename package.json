{
  "name": "cindra-mod",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:integration": "factorio-test run -p .",
    "test:integration:graphics": "factorio-test run -p . -g",
    "test:unit": "cd mods/cindra && for f in unit-tests/test_*.lua; do echo \"# $f\" && lua \"$f\" || exit 1; done",
    "test": "npm run test:unit && npm run test:integration"
  },
  "devDependencies": {
    "factorio-test-cli": "^3.0.0"
  }
}
