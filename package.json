{
  "name": "cindra-mod",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:integration": "cindra-test",
    "test:integration:graphics": "cindra-test -g",
    "test:unit": "cd mods/cindra && for f in unit-tests/test_*.lua; do echo \"# $f\" && lua \"$f\" || exit 1; done",
    "test:play": "bash tests/play-sh.test.sh && bash tests/play-patchelf.test.sh",
    "test": "npm run test:unit && npm run test:play && npm run test:integration"
  },
  "devDependencies": {
    "factorio-test-cli": "^3.0.0"
  }
}
