node_modules/
*.zip
*.tar.xz
.factorio/
.factorio-test/
factorio-test-data-dir/
factorio-test-output.json
package-lock.json
mods-bundle/
# Mods play.sh fetches from the portal (any-planet-start, helmod) are cached
# here so we download each at most once. Never committed.
.play-cache/
# Local Factorio install (./play.sh + test runner). The DLCs + binary live
# here; nothing in this directory should be committed. Ignored both as a real
# directory and as a local symlink to an out-of-repo install.
factorio/
factorio
node_modules
fbsr-renderer/
# Python bytecode cache (from the importable art pipeline + its pixel test).
__pycache__/

# nix build output symlinks (`nix build` writes ./result -> /nix/store/...).
result
result-*

# Gas Town / agent runtime state (local only).
.beads/
.claude/
.runtime/
CLAUDE.md
